import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Pure API server — no UI pages needed
  // Node.js runtime is enforced via vercel.json for Vercel deployment
};

export default nextConfig;
